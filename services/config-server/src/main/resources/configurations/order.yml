server:
  port: ${ORDER_SERVICE_PORT:8040}

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/orders
    username: johnny
    password: johnny123
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serizalizer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: orderConfirmation:com.e_commerce.kafka.OrderConfirmation

application:
  config:
    customer-url: http://localhost:8080/api/v1/customers
    product-url: http://localhost:8080/api/v1/products
    payment-url: http://localhost:8080/api/v1/payments